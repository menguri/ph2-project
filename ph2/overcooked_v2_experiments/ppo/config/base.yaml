defaults:
  # - env: default
  - wandb: default
  - model: cnn

SEED: 42
NUM_SEEDS: 10
TUNE: False
NUM_CHECKPOINTS: 3
# Eval ckpt 기본값: 1M env step cadence 로 eval/run_X/ 에 저장. baseline 과 동일.
PH1_EVAL_EVERY_ENV_STEPS: 1000000
SAVE_EVAL_CHECKPOINTS: True
VISUALIZE: False
OLD_OVERCOOKED: False
DISABLE_OLD_OVERCOOKED_AUTO: False

# NUM_ITERATIONS: 10
